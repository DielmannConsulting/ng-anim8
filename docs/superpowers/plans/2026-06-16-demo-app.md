# Demo App Redesign & GitHub Pages Deployment — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the bare-bones demo app with a modern Gradient & Glass showcase of all six ng-anim8 animation components and add GitHub Pages deployment.

**Architecture:** Two-pane Angular app — fixed sidebar with IntersectionObserver-based active link tracking, scrollable right pane with a hero section and one component section each for Fade, Slide, Collapse, Grow, Zoom, Stagger. All section components use global shared styles from `styles.scss`; component-scoped SCSS is kept minimal to stay within the 8kB budget.

**Tech Stack:** Angular 20, ng-anim8 (workspace), Prism.js (HTML highlighting), GitHub Actions Pages deployment.

---

## File Map

**Create:**
- `projects/demo/src/app/shared/code-snippet/code-snippet.component.ts`
- `projects/demo/src/app/shared/code-snippet/code-snippet.component.scss`
- `projects/demo/src/app/sections/fade/fade-section.component.ts`
- `projects/demo/src/app/sections/fade/fade-section.component.html`
- `projects/demo/src/app/sections/slide/slide-section.component.ts`
- `projects/demo/src/app/sections/slide/slide-section.component.html`
- `projects/demo/src/app/sections/collapse/collapse-section.component.ts`
- `projects/demo/src/app/sections/collapse/collapse-section.component.html`
- `projects/demo/src/app/sections/grow/grow-section.component.ts`
- `projects/demo/src/app/sections/grow/grow-section.component.html`
- `projects/demo/src/app/sections/zoom/zoom-section.component.ts`
- `projects/demo/src/app/sections/zoom/zoom-section.component.html`
- `projects/demo/src/app/sections/stagger/stagger-section.component.ts`
- `projects/demo/src/app/sections/stagger/stagger-section.component.html`
- `projects/demo/src/app/layout/layout.component.ts`
- `projects/demo/src/app/layout/layout.component.html`
- `projects/demo/src/app/layout/layout.component.scss`

**Modify:**
- `angular.json` — add Prism tomorrow CSS to demo styles array
- `projects/demo/src/styles.scss` — CSS variables, resets, shared section/button/picker styles
- `projects/demo/src/app/app.ts` — import and use `LayoutComponent`
- `projects/demo/src/app/app.html` — replace with `<app-layout />`
- `projects/demo/src/app/app.scss` — clear it
- `projects/demo/src/app/app.spec.ts` — update tests
- `.github/workflows/ci.yml` — add Pages deploy job
- `README.md` — replace live demo placeholder URL

---

## Task 1: Install Prism.js and configure angular.json

**Files:**
- Modify: `angular.json`

- [ ] **Step 1: Install runtime and types**

```bash
pnpm add prismjs
pnpm add -D @types/prismjs
```

Expected: both added without errors, `node_modules/prismjs/themes/prism-tomorrow.css` exists.

- [ ] **Step 2: Verify the file path**

```bash
ls node_modules/prismjs/themes/prism-tomorrow.css
```

Expected: file listed.

- [ ] **Step 3: Add Prism CSS to the demo styles array in angular.json**

In `angular.json`, find the two `"styles"` arrays under `projects.demo.architect` (one under `build.options`, one under `test.options`). Update both from:

```json
"styles": [
  "projects/demo/src/styles.scss"
]
```

to:

```json
"styles": [
  "node_modules/prismjs/themes/prism-tomorrow.css",
  "projects/demo/src/styles.scss"
]
```

- [ ] **Step 4: Commit**

```bash
git add angular.json package.json pnpm-lock.yaml
git commit -m "build(demo): add prismjs dependency"
```

---

## Task 2: Global styles

**Files:**
- Modify: `projects/demo/src/styles.scss`

- [ ] **Step 1: Replace the contents of `projects/demo/src/styles.scss`**

```scss
:root {
  --cyan: #38bdf8;
  --purple: #c084fc;
  --text: #e2e8f0;
  --text-dim: #94a3b8;
  --muted: #64748b;
  --glass-fill: rgba(255, 255, 255, 0.05);
  --glass-border: rgba(255, 255, 255, 0.08);
  --code-bg: #111827;
}

*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  background: #0d1117;
  color: var(--text);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  line-height: 1.6;
  background-image:
    radial-gradient(ellipse at 15% 5%, rgba(56, 189, 248, 0.07) 0%, transparent 55%),
    radial-gradient(ellipse at 85% 95%, rgba(168, 85, 247, 0.07) 0%, transparent 55%);
  background-attachment: fixed;
}

/* ── Section layout ──────────────────────────────────── */

.anim-section {
  padding: 4rem 0;
  border-bottom: 1px solid var(--glass-border);
  scroll-margin-top: 2rem;

  &:last-child {
    border-bottom: none;
  }
}

.anim-section__header {
  margin-bottom: 1.5rem;
}

.anim-section__title {
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--text);
  margin-bottom: 0.375rem;
}

.anim-section__desc {
  color: var(--muted);
  font-size: 0.9rem;
}

.anim-section__controls {
  display: flex;
  align-items: center;
  gap: 1rem;
  flex-wrap: wrap;
  margin-bottom: 1.25rem;
}

.anim-section__preview {
  background: var(--glass-fill);
  border: 1px solid var(--glass-border);
  border-radius: 12px;
  padding: 2rem;
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  backdrop-filter: blur(12px);
  margin-bottom: 1rem;
}

/* ── Preview content box ─────────────────────────────── */

.preview-box {
  background: linear-gradient(
    135deg,
    rgba(56, 189, 248, 0.12),
    rgba(168, 85, 247, 0.12)
  );
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 8px;
  padding: 1.25rem 2rem;
  color: var(--text);
  font-size: 0.95rem;
  text-align: center;
}

/* ── Toggle button ───────────────────────────────────── */

.btn-toggle {
  background: rgba(56, 189, 248, 0.12);
  border: 1px solid rgba(56, 189, 248, 0.3);
  border-radius: 8px;
  color: var(--cyan);
  cursor: pointer;
  font-size: 0.875rem;
  font-weight: 600;
  padding: 0.5rem 1.25rem;
  transition: background 0.15s;

  &:hover {
    background: rgba(56, 189, 248, 0.22);
  }

  &:active {
    transform: scale(0.97);
  }
}

/* ── Duration / direction picker ─────────────────────── */

.picker {
  display: flex;
  gap: 2px;
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid var(--glass-border);
  border-radius: 8px;
  padding: 3px;
}

.picker__btn {
  background: transparent;
  border: none;
  border-radius: 6px;
  color: var(--muted);
  cursor: pointer;
  font-size: 0.8rem;
  padding: 0.3rem 0.65rem;
  transition: background 0.15s, color 0.15s;

  &:hover {
    color: var(--text);
  }

  &--active {
    background: rgba(56, 189, 248, 0.15);
    color: var(--cyan);
  }
}

/* ── Prism overrides (force our code-bg color) ───────── */

pre[class*='language-'] {
  background: var(--code-bg) !important;
  border: 1px solid var(--glass-border);
  border-radius: 8px;
  font-size: 0.83rem;
  margin: 0;
}
```

- [ ] **Step 2: Commit**

```bash
git add projects/demo/src/styles.scss
git commit -m "feat(demo): add global CSS variables and shared section styles"
```

---

## Task 3: CodeSnippetComponent

**Files:**
- Create: `projects/demo/src/app/shared/code-snippet/code-snippet.component.ts`
- Create: `projects/demo/src/app/shared/code-snippet/code-snippet.component.scss`

- [ ] **Step 1: Create the component**

`projects/demo/src/app/shared/code-snippet/code-snippet.component.ts`:

```typescript
import { Component, ElementRef, effect, inject, input } from '@angular/core';
import Prism from 'prismjs';
import 'prismjs/components/prism-markup';

@Component({
  selector: 'app-code-snippet',
  standalone: true,
  template: `<pre class="language-html"><code #codeEl class="language-html"></code></pre>`,
  styleUrl: './code-snippet.component.scss',
})
export class CodeSnippetComponent {
  code = input.required<string>();

  private el = inject(ElementRef);

  constructor() {
    effect(() => {
      const text = this.code();
      const codeEl: HTMLElement | null = this.el.nativeElement.querySelector('code');
      if (!codeEl) return;
      codeEl.textContent = text;
      Prism.highlightElement(codeEl);
    });
  }
}
```

- [ ] **Step 2: Create the stylesheet**

`projects/demo/src/app/shared/code-snippet/code-snippet.component.scss`:

```scss
:host {
  display: block;
}
```

- [ ] **Step 3: Build to confirm no type errors**

```bash
pnpm ng build ng-anim8 --watch=false 2>&1 | tail -5
pnpm ng build demo --watch=false 2>&1 | tail -10
```

Expected: both exit 0, no TypeScript errors. (Ignore bundle size warnings for now.)

- [ ] **Step 4: Commit**

```bash
git add projects/demo/src/app/shared/
git commit -m "feat(demo): add CodeSnippetComponent with Prism.js highlighting"
```

---

## Task 4: FadeSectionComponent

**Files:**
- Create: `projects/demo/src/app/sections/fade/fade-section.component.ts`
- Create: `projects/demo/src/app/sections/fade/fade-section.component.html`

This task also establishes the repeatable section pattern used in Tasks 5–8.

- [ ] **Step 1: Create the component**

`projects/demo/src/app/sections/fade/fade-section.component.ts`:

```typescript
import { Component, computed, signal } from '@angular/core';
import { FadeComponent } from 'ng-anim8';
import type { Duration } from 'ng-anim8';
import { CodeSnippetComponent } from '../../shared/code-snippet/code-snippet.component';

@Component({
  selector: 'app-fade-section',
  standalone: true,
  imports: [FadeComponent, CodeSnippetComponent],
  templateUrl: './fade-section.component.html',
})
export class FadeSectionComponent {
  show     = signal(false);
  duration = signal<Duration>('normal');

  readonly durations: Duration[] = ['fast', 'normal', 'slow'];

  code = computed(() => `<anim8-fade
  [show]="visible()"
  duration="${this.duration()}">
  <div>Your content</div>
</anim8-fade>`);
}
```

- [ ] **Step 2: Create the template**

`projects/demo/src/app/sections/fade/fade-section.component.html`:

```html
<section id="fade" class="anim-section">
  <div class="anim-section__header">
    <h2 class="anim-section__title">Fade</h2>
    <p class="anim-section__desc">Opacity 0 → 1 with configurable easing</p>
  </div>

  <div class="anim-section__controls">
    <button class="btn-toggle" (click)="show.set(!show())">
      {{ show() ? 'Hide' : 'Show' }}
    </button>
    <div class="picker">
      @for (d of durations; track d) {
        <button
          class="picker__btn"
          [class.picker__btn--active]="duration() === d"
          (click)="duration.set(d)">{{ d }}</button>
      }
    </div>
  </div>

  <div class="anim-section__preview">
    <anim8-fade [show]="show()" [duration]="duration()">
      <div class="preview-box">Fade content</div>
    </anim8-fade>
  </div>

  <app-code-snippet [code]="code()" />
</section>
```

- [ ] **Step 3: Build to confirm**

```bash
pnpm ng build demo --watch=false 2>&1 | tail -10
```

Expected: exits 0.

- [ ] **Step 4: Commit**

```bash
git add projects/demo/src/app/sections/fade/
git commit -m "feat(demo): add FadeSectionComponent"
```

---

## Task 5: SlideSectionComponent

**Files:**
- Create: `projects/demo/src/app/sections/slide/slide-section.component.ts`
- Create: `projects/demo/src/app/sections/slide/slide-section.component.html`

Slide adds a direction picker in addition to the duration picker.

- [ ] **Step 1: Create the component**

`projects/demo/src/app/sections/slide/slide-section.component.ts`:

```typescript
import { Component, computed, signal } from '@angular/core';
import { SlideComponent } from 'ng-anim8';
import type { Duration, SlideDirection } from 'ng-anim8';
import { CodeSnippetComponent } from '../../shared/code-snippet/code-snippet.component';

@Component({
  selector: 'app-slide-section',
  standalone: true,
  imports: [SlideComponent, CodeSnippetComponent],
  templateUrl: './slide-section.component.html',
})
export class SlideSectionComponent {
  show      = signal(false);
  duration  = signal<Duration>('normal');
  direction = signal<SlideDirection>('up');

  readonly durations: Duration[]        = ['fast', 'normal', 'slow'];
  readonly directions: SlideDirection[] = ['up', 'down', 'left', 'right'];

  readonly directionLabels: Record<SlideDirection, string> = {
    up: '↑ up', down: '↓ down', left: '← left', right: '→ right',
  };

  code = computed(() => `<anim8-slide
  [show]="visible()"
  direction="${this.direction()}"
  duration="${this.duration()}">
  <div>Your content</div>
</anim8-slide>`);
}
```

- [ ] **Step 2: Create the template**

`projects/demo/src/app/sections/slide/slide-section.component.html`:

```html
<section id="slide" class="anim-section">
  <div class="anim-section__header">
    <h2 class="anim-section__title">Slide</h2>
    <p class="anim-section__desc">Translate + opacity in 4 directions</p>
  </div>

  <div class="anim-section__controls">
    <button class="btn-toggle" (click)="show.set(!show())">
      {{ show() ? 'Hide' : 'Show' }}
    </button>
    <div class="picker">
      @for (d of durations; track d) {
        <button
          class="picker__btn"
          [class.picker__btn--active]="duration() === d"
          (click)="duration.set(d)">{{ d }}</button>
      }
    </div>
    <div class="picker">
      @for (dir of directions; track dir) {
        <button
          class="picker__btn"
          [class.picker__btn--active]="direction() === dir"
          (click)="direction.set(dir)">{{ directionLabels[dir] }}</button>
      }
    </div>
  </div>

  <div class="anim-section__preview">
    <anim8-slide [show]="show()" [duration]="duration()" [direction]="direction()">
      <div class="preview-box">Slide content</div>
    </anim8-slide>
  </div>

  <app-code-snippet [code]="code()" />
</section>
```

- [ ] **Step 3: Build to confirm**

```bash
pnpm ng build demo --watch=false 2>&1 | tail -10
```

Expected: exits 0.

- [ ] **Step 4: Commit**

```bash
git add projects/demo/src/app/sections/slide/
git commit -m "feat(demo): add SlideSectionComponent"
```

---

## Task 6: CollapseSectionComponent

**Files:**
- Create: `projects/demo/src/app/sections/collapse/collapse-section.component.ts`
- Create: `projects/demo/src/app/sections/collapse/collapse-section.component.html`

- [ ] **Step 1: Create the component**

`projects/demo/src/app/sections/collapse/collapse-section.component.ts`:

```typescript
import { Component, computed, signal } from '@angular/core';
import { CollapseComponent } from 'ng-anim8';
import type { Duration } from 'ng-anim8';
import { CodeSnippetComponent } from '../../shared/code-snippet/code-snippet.component';

@Component({
  selector: 'app-collapse-section',
  standalone: true,
  imports: [CollapseComponent, CodeSnippetComponent],
  templateUrl: './collapse-section.component.html',
})
export class CollapseSectionComponent {
  show     = signal(false);
  duration = signal<Duration>('normal');

  readonly durations: Duration[] = ['fast', 'normal', 'slow'];

  code = computed(() => `<anim8-collapse
  [show]="visible()"
  duration="${this.duration()}">
  <div>Your content</div>
</anim8-collapse>`);
}
```

- [ ] **Step 2: Create the template**

`projects/demo/src/app/sections/collapse/collapse-section.component.html`:

```html
<section id="collapse" class="anim-section">
  <div class="anim-section__header">
    <h2 class="anim-section__title">Collapse</h2>
    <p class="anim-section__desc">Height 0 → auto via CSS grid trick — no JS measurement</p>
  </div>

  <div class="anim-section__controls">
    <button class="btn-toggle" (click)="show.set(!show())">
      {{ show() ? 'Hide' : 'Show' }}
    </button>
    <div class="picker">
      @for (d of durations; track d) {
        <button
          class="picker__btn"
          [class.picker__btn--active]="duration() === d"
          (click)="duration.set(d)">{{ d }}</button>
      }
    </div>
  </div>

  <div class="anim-section__preview">
    <anim8-collapse [show]="show()" [duration]="duration()">
      <div class="preview-box">Collapse content</div>
    </anim8-collapse>
  </div>

  <app-code-snippet [code]="code()" />
</section>
```

- [ ] **Step 3: Commit**

```bash
git add projects/demo/src/app/sections/collapse/
git commit -m "feat(demo): add CollapseSectionComponent"
```

---

## Task 7: GrowSectionComponent

**Files:**
- Create: `projects/demo/src/app/sections/grow/grow-section.component.ts`
- Create: `projects/demo/src/app/sections/grow/grow-section.component.html`

- [ ] **Step 1: Create the component**

`projects/demo/src/app/sections/grow/grow-section.component.ts`:

```typescript
import { Component, computed, signal } from '@angular/core';
import { GrowComponent } from 'ng-anim8';
import type { Duration } from 'ng-anim8';
import { CodeSnippetComponent } from '../../shared/code-snippet/code-snippet.component';

@Component({
  selector: 'app-grow-section',
  standalone: true,
  imports: [GrowComponent, CodeSnippetComponent],
  templateUrl: './grow-section.component.html',
})
export class GrowSectionComponent {
  show     = signal(false);
  duration = signal<Duration>('normal');

  readonly durations: Duration[] = ['fast', 'normal', 'slow'];

  code = computed(() => `<anim8-grow
  [show]="visible()"
  duration="${this.duration()}">
  <div>Your content</div>
</anim8-grow>`);
}
```

- [ ] **Step 2: Create the template**

`projects/demo/src/app/sections/grow/grow-section.component.html`:

```html
<section id="grow" class="anim-section">
  <div class="anim-section__header">
    <h2 class="anim-section__title">Grow</h2>
    <p class="anim-section__desc">Scale 0.75 → 1 combined with fade</p>
  </div>

  <div class="anim-section__controls">
    <button class="btn-toggle" (click)="show.set(!show())">
      {{ show() ? 'Hide' : 'Show' }}
    </button>
    <div class="picker">
      @for (d of durations; track d) {
        <button
          class="picker__btn"
          [class.picker__btn--active]="duration() === d"
          (click)="duration.set(d)">{{ d }}</button>
      }
    </div>
  </div>

  <div class="anim-section__preview">
    <anim8-grow [show]="show()" [duration]="duration()">
      <div class="preview-box">Grow content</div>
    </anim8-grow>
  </div>

  <app-code-snippet [code]="code()" />
</section>
```

- [ ] **Step 3: Commit**

```bash
git add projects/demo/src/app/sections/grow/
git commit -m "feat(demo): add GrowSectionComponent"
```

---

## Task 8: ZoomSectionComponent

**Files:**
- Create: `projects/demo/src/app/sections/zoom/zoom-section.component.ts`
- Create: `projects/demo/src/app/sections/zoom/zoom-section.component.html`

- [ ] **Step 1: Create the component**

`projects/demo/src/app/sections/zoom/zoom-section.component.ts`:

```typescript
import { Component, computed, signal } from '@angular/core';
import { ZoomComponent } from 'ng-anim8';
import type { Duration } from 'ng-anim8';
import { CodeSnippetComponent } from '../../shared/code-snippet/code-snippet.component';

@Component({
  selector: 'app-zoom-section',
  standalone: true,
  imports: [ZoomComponent, CodeSnippetComponent],
  templateUrl: './zoom-section.component.html',
})
export class ZoomSectionComponent {
  show     = signal(false);
  duration = signal<Duration>('normal');

  readonly durations: Duration[] = ['fast', 'normal', 'slow'];

  code = computed(() => `<anim8-zoom
  [show]="visible()"
  duration="${this.duration()}">
  <div>Your content</div>
</anim8-zoom>`);
}
```

- [ ] **Step 2: Create the template**

`projects/demo/src/app/sections/zoom/zoom-section.component.html`:

```html
<section id="zoom" class="anim-section">
  <div class="anim-section__header">
    <h2 class="anim-section__title">Zoom</h2>
    <p class="anim-section__desc">Scale 0 → 1 — sharp pop-in effect</p>
  </div>

  <div class="anim-section__controls">
    <button class="btn-toggle" (click)="show.set(!show())">
      {{ show() ? 'Hide' : 'Show' }}
    </button>
    <div class="picker">
      @for (d of durations; track d) {
        <button
          class="picker__btn"
          [class.picker__btn--active]="duration() === d"
          (click)="duration.set(d)">{{ d }}</button>
      }
    </div>
  </div>

  <div class="anim-section__preview">
    <anim8-zoom [show]="show()" [duration]="duration()">
      <div class="preview-box">Zoom content</div>
    </anim8-zoom>
  </div>

  <app-code-snippet [code]="code()" />
</section>
```

- [ ] **Step 3: Commit**

```bash
git add projects/demo/src/app/sections/zoom/
git commit -m "feat(demo): add ZoomSectionComponent"
```

---

## Task 9: StaggerSectionComponent

**Files:**
- Create: `projects/demo/src/app/sections/stagger/stagger-section.component.ts`
- Create: `projects/demo/src/app/sections/stagger/stagger-section.component.html`

Stagger has no `show` toggle — it uses an item list with Add/Reset buttons.

- [ ] **Step 1: Create the component**

`projects/demo/src/app/sections/stagger/stagger-section.component.ts`:

```typescript
import { Component } from '@angular/core';
import { signal } from '@angular/core';
import { StaggerComponent } from 'ng-anim8';
import { CodeSnippetComponent } from '../../shared/code-snippet/code-snippet.component';

@Component({
  selector: 'app-stagger-section',
  standalone: true,
  imports: [StaggerComponent, CodeSnippetComponent],
  templateUrl: './stagger-section.component.html',
})
export class StaggerSectionComponent {
  items = signal<string[]>([]);

  private nextId = 1;

  addItem(): void {
    this.items.update(list => [...list, `Item ${this.nextId++}`]);
  }

  reset(): void {
    this.items.set([]);
    this.nextId = 1;
  }

  readonly code = `<anim8-stagger [gap]="75">
  @for (item of items(); track item) {
    <div>{{ item }}</div>
  }
</anim8-stagger>`;
}
```

- [ ] **Step 2: Create the template**

`projects/demo/src/app/sections/stagger/stagger-section.component.html`:

```html
<section id="stagger" class="anim-section">
  <div class="anim-section__header">
    <h2 class="anim-section__title">Stagger</h2>
    <p class="anim-section__desc">Staggered enter delay on list children via MutationObserver</p>
  </div>

  <div class="anim-section__controls">
    <button class="btn-toggle" (click)="addItem()">Add item</button>
    <button class="btn-toggle" (click)="reset()" style="opacity: 0.6">Reset</button>
  </div>

  <div class="anim-section__preview" style="flex-direction: column; align-items: stretch; gap: 0.5rem">
    <anim8-stagger [gap]="75">
      @for (item of items(); track item) {
        <div class="preview-box" style="margin-bottom: 0.5rem">{{ item }}</div>
      }
    </anim8-stagger>
  </div>

  <app-code-snippet [code]="code" />
</section>
```

- [ ] **Step 3: Commit**

```bash
git add projects/demo/src/app/sections/stagger/
git commit -m "feat(demo): add StaggerSectionComponent"
```

---

## Task 10: LayoutComponent

**Files:**
- Create: `projects/demo/src/app/layout/layout.component.ts`
- Create: `projects/demo/src/app/layout/layout.component.html`
- Create: `projects/demo/src/app/layout/layout.component.scss`

Contains the fixed sidebar, hero section, and wires all section components together.

- [ ] **Step 1: Create the component**

`projects/demo/src/app/layout/layout.component.ts`:

```typescript
import { Component, AfterViewInit, OnDestroy, signal } from '@angular/core';
import { FadeSectionComponent }     from '../sections/fade/fade-section.component';
import { SlideSectionComponent }    from '../sections/slide/slide-section.component';
import { CollapseSectionComponent } from '../sections/collapse/collapse-section.component';
import { GrowSectionComponent }     from '../sections/grow/grow-section.component';
import { ZoomSectionComponent }     from '../sections/zoom/zoom-section.component';
import { StaggerSectionComponent }  from '../sections/stagger/stagger-section.component';

@Component({
  selector: 'app-layout',
  standalone: true,
  imports: [
    FadeSectionComponent,
    SlideSectionComponent,
    CollapseSectionComponent,
    GrowSectionComponent,
    ZoomSectionComponent,
    StaggerSectionComponent,
  ],
  templateUrl: './layout.component.html',
  styleUrl: './layout.component.scss',
})
export class LayoutComponent implements AfterViewInit, OnDestroy {
  activeSection = signal<string>('fade');
  copied        = signal(false);

  private observer: IntersectionObserver | null = null;

  readonly navItems = [
    { id: 'fade',     label: 'Fade' },
    { id: 'slide',    label: 'Slide' },
    { id: 'collapse', label: 'Collapse' },
    { id: 'grow',     label: 'Grow' },
    { id: 'zoom',     label: 'Zoom' },
    { id: 'stagger',  label: 'Stagger' },
  ];

  ngAfterViewInit(): void {
    if (typeof IntersectionObserver === 'undefined') return;

    this.observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (entry.isIntersecting) {
            this.activeSection.set(entry.target.id);
            break;
          }
        }
      },
      { rootMargin: '-10% 0px -80% 0px' },
    );

    document.querySelectorAll('section.anim-section[id]').forEach(el => {
      this.observer!.observe(el);
    });
  }

  ngOnDestroy(): void {
    this.observer?.disconnect();
  }

  copyInstall(): void {
    navigator.clipboard?.writeText('npm install ng-anim8').then(() => {
      this.copied.set(true);
      setTimeout(() => this.copied.set(false), 2000);
    });
  }
}
```

- [ ] **Step 2: Create the template**

`projects/demo/src/app/layout/layout.component.html`:

```html
<div class="layout">
  <!-- ── Sidebar ── -->
  <aside class="sidebar">
    <div class="sidebar__logo">
      <span class="sidebar__dot"></span>
      <span class="sidebar__name">ng-anim8</span>
    </div>

    <button class="sidebar__install" (click)="copyInstall()">
      {{ copied() ? '✓ Copied!' : 'npm install ng-anim8' }}
    </button>

    <nav class="sidebar__nav">
      @for (item of navItems; track item.id) {
        <a
          class="sidebar__link"
          [class.sidebar__link--active]="activeSection() === item.id"
          [href]="'#' + item.id"
        >{{ item.label }}</a>
      }
    </nav>

    <a
      class="sidebar__github"
      href="https://github.com/DielmannConsulting/ng-anim8"
      target="_blank"
      rel="noopener noreferrer"
    >GitHub →</a>
  </aside>

  <!-- ── Scrollable content ── -->
  <main class="content">
    <!-- Hero -->
    <section class="hero">
      <p class="hero__eyebrow">Angular 20+ · CSS-powered · SSR-safe</p>
      <h1 class="hero__title">
        Animations that<br>
        <span class="hero__gradient">feel alive</span>
      </h1>
      <p class="hero__sub">
        Declarative animation components for Angular. Wrap any content,
        bind <code class="hero__code">[show]</code>, done.
      </p>
      <div class="hero__actions">
        <button class="hero__install-btn" (click)="copyInstall()">
          {{ copied() ? '✓ Copied!' : 'npm install ng-anim8' }}
        </button>
        <a
          class="hero__github-btn"
          href="https://github.com/DielmannConsulting/ng-anim8"
          target="_blank"
          rel="noopener noreferrer"
        >View on GitHub →</a>
      </div>
      <div class="hero__badges">
        <img src="https://img.shields.io/npm/v/ng-anim8.svg" alt="npm version">
        <img src="https://github.com/DielmannConsulting/ng-anim8/actions/workflows/ci.yml/badge.svg" alt="CI">
        <img src="https://img.shields.io/badge/License-MIT-blue.svg" alt="License: MIT">
      </div>
    </section>

    <!-- Animation sections -->
    <app-fade-section />
    <app-slide-section />
    <app-collapse-section />
    <app-grow-section />
    <app-zoom-section />
    <app-stagger-section />
  </main>
</div>
```

- [ ] **Step 3: Create the stylesheet**

`projects/demo/src/app/layout/layout.component.scss`:

```scss
/* ── Two-pane shell ─────────────────────────────────── */

.layout {
  display: flex;
  min-height: 100vh;
}

/* ── Sidebar ────────────────────────────────────────── */

.sidebar {
  position: fixed;
  top: 0;
  left: 0;
  width: 220px;
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: rgba(255, 255, 255, 0.025);
  border-right: 1px solid rgba(255, 255, 255, 0.08);
  padding: 2rem 1.25rem;
  z-index: 10;
  overflow-y: auto;
}

.sidebar__logo {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-bottom: 1.25rem;
}

.sidebar__dot {
  width: 10px;
  height: 10px;
  background: linear-gradient(135deg, #38bdf8, #c084fc);
  border-radius: 50%;
  flex-shrink: 0;
}

.sidebar__name {
  color: #e2e8f0;
  font-size: 1rem;
  font-weight: 700;
  letter-spacing: 0.04em;
}

.sidebar__install {
  background: rgba(56, 189, 248, 0.07);
  border: 1px solid rgba(56, 189, 248, 0.2);
  border-radius: 6px;
  color: #38bdf8;
  cursor: pointer;
  font-family: monospace;
  font-size: 0.75rem;
  margin-bottom: 1.5rem;
  padding: 0.5rem 0.75rem;
  text-align: left;
  transition: background 0.15s;
  width: 100%;

  &:hover {
    background: rgba(56, 189, 248, 0.14);
  }
}

.sidebar__nav {
  display: flex;
  flex-direction: column;
  gap: 2px;
  flex: 1;
}

.sidebar__link {
  border-left: 2px solid transparent;
  border-radius: 0 6px 6px 0;
  color: #64748b;
  display: block;
  font-size: 0.875rem;
  padding: 0.45rem 0.75rem;
  text-decoration: none;
  transition: color 0.15s, background 0.15s, border-color 0.15s;

  &:hover {
    background: rgba(255, 255, 255, 0.04);
    color: #e2e8f0;
  }

  &--active {
    background: rgba(56, 189, 248, 0.07);
    border-left-color: #38bdf8;
    color: #38bdf8;
  }
}

.sidebar__github {
  color: #64748b;
  font-size: 0.85rem;
  padding: 0.45rem 0.75rem;
  text-decoration: none;

  &:hover {
    color: #e2e8f0;
  }
}

/* ── Scrollable content ─────────────────────────────── */

.content {
  margin-left: 220px;
  flex: 1;
  max-width: 860px;
  padding: 0 3rem 5rem;
}

/* ── Hero ───────────────────────────────────────────── */

.hero {
  padding: 5rem 0 4rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  margin-bottom: 0;
}

.hero__eyebrow {
  color: #38bdf8;
  font-size: 0.8rem;
  font-weight: 600;
  letter-spacing: 0.08em;
  margin-bottom: 1.25rem;
  text-transform: uppercase;
}

.hero__title {
  font-size: clamp(2.5rem, 5vw, 3.75rem);
  font-weight: 800;
  line-height: 1.1;
  margin-bottom: 1.25rem;
}

.hero__gradient {
  background: linear-gradient(90deg, #38bdf8, #c084fc);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.hero__sub {
  color: #94a3b8;
  font-size: 1.1rem;
  max-width: 520px;
  margin-bottom: 2rem;
}

.hero__code {
  background: rgba(56, 189, 248, 0.1);
  border-radius: 4px;
  color: #38bdf8;
  font-family: monospace;
  padding: 0.1em 0.4em;
}

.hero__actions {
  display: flex;
  gap: 0.75rem;
  flex-wrap: wrap;
  margin-bottom: 1.75rem;
}

.hero__install-btn {
  background: rgba(56, 189, 248, 0.12);
  border: 1px solid rgba(56, 189, 248, 0.35);
  border-radius: 8px;
  color: #38bdf8;
  cursor: pointer;
  font-family: monospace;
  font-size: 0.9rem;
  font-weight: 600;
  padding: 0.6rem 1.25rem;
  transition: background 0.15s;

  &:hover {
    background: rgba(56, 189, 248, 0.22);
  }
}

.hero__github-btn {
  background: rgba(255, 255, 255, 0.06);
  border: 1px solid rgba(255, 255, 255, 0.12);
  border-radius: 8px;
  color: #e2e8f0;
  font-size: 0.9rem;
  font-weight: 600;
  padding: 0.6rem 1.25rem;
  text-decoration: none;
  transition: background 0.15s;

  &:hover {
    background: rgba(255, 255, 255, 0.1);
  }
}

.hero__badges {
  display: flex;
  gap: 0.5rem;
  flex-wrap: wrap;

  img {
    height: 20px;
  }
}
```

- [ ] **Step 4: Build to confirm**

```bash
pnpm ng build demo --watch=false 2>&1 | tail -10
```

Expected: exits 0.

- [ ] **Step 5: Commit**

```bash
git add projects/demo/src/app/layout/
git commit -m "feat(demo): add LayoutComponent with sidebar, hero, and section wiring"
```

---

## Task 11: Update App root

**Files:**
- Modify: `projects/demo/src/app/app.ts`
- Modify: `projects/demo/src/app/app.html`
- Modify: `projects/demo/src/app/app.scss`
- Modify: `projects/demo/src/app/app.spec.ts`

- [ ] **Step 1: Replace app.ts**

`projects/demo/src/app/app.ts`:

```typescript
import { Component } from '@angular/core';
import { LayoutComponent } from './layout/layout.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [LayoutComponent],
  template: '<app-layout />',
  styles: [],
})
export class App {}
```

- [ ] **Step 2: Clear app.html**

Delete the file — the template is now inline:

```bash
rm projects/demo/src/app/app.html
```

If the CLI complains about a missing `templateUrl`, confirm the component uses `template:` (inline) not `templateUrl:`.

- [ ] **Step 3: Clear app.scss**

Replace `projects/demo/src/app/app.scss` with an empty file:

```scss
```

- [ ] **Step 4: Update app.spec.ts**

`projects/demo/src/app/app.spec.ts`:

```typescript
import { TestBed } from '@angular/core/testing';
import { App } from './app';

(window as any).IntersectionObserver = class {
  observe()    {}
  disconnect() {}
  unobserve()  {}
};

describe('App', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [App],
    }).compileComponents();
  });

  it('should create the app', () => {
    const fixture = TestBed.createComponent(App);
    expect(fixture.componentInstance).toBeTruthy();
  });

  it('should render the layout', () => {
    const fixture = TestBed.createComponent(App);
    fixture.detectChanges();
    const el = fixture.nativeElement as HTMLElement;
    expect(el.querySelector('app-layout')).not.toBeNull();
  });
});
```

- [ ] **Step 5: Run library tests to confirm nothing broke**

```bash
pnpm ng test ng-anim8 --watch=false
```

Expected: all tests pass.

- [ ] **Step 6: Build the demo**

```bash
pnpm ng build demo --watch=false 2>&1 | tail -10
```

Expected: exits 0.

- [ ] **Step 7: Serve and visually verify the demo**

```bash
pnpm start
```

Open http://localhost:4200. Verify:
- Sidebar appears on the left with all 6 nav links
- Hero section shows gradient headline, install button, badges
- Each animation section is visible when scrolled to
- Toggle buttons trigger real animations
- Duration picker changes animation speed
- Slide direction picker changes direction
- Stagger "Add item" adds animated items
- Code snippets update when duration/direction change

- [ ] **Step 8: Commit**

```bash
git add projects/demo/src/app/app.ts projects/demo/src/app/app.scss projects/demo/src/app/app.spec.ts
git rm --cached projects/demo/src/app/app.html 2>/dev/null; true
git commit -m "feat(demo): wire LayoutComponent into app root"
```

---

## Task 12: GitHub Pages deploy job

**Files:**
- Modify: `.github/workflows/ci.yml`

- [ ] **Step 1: Add the deploy job to ci.yml**

Append the following `deploy` job to `.github/workflows/ci.yml`. It sits at the same indentation level as `build-and-test` and runs only on pushes to `main` after CI passes.

The final file should look like this:

```yaml
name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Test
        run: pnpm ng test ng-anim8 --watch=false

      - name: Build library
        run: pnpm ng build ng-anim8

  deploy:
    name: Deploy to GitHub Pages
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build demo app
        run: pnpm ng build demo --base-href /ng-anim8/

      - name: Copy index.html to 404.html
        run: cp dist/demo/browser/index.html dist/demo/browser/404.html

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/demo/browser

      - id: deployment
        name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: Enable GitHub Pages in the repo settings**

In the GitHub repository settings → Pages → Source: select **"GitHub Actions"** (not a branch). This only needs to be done once manually.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add GitHub Pages deploy job for demo app"
```

---

## Task 13: Update README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace the live demo placeholder in README.md**

Find the line:

```markdown
**[Live Demo](#)** <!-- replace with your GitHub Pages or StackBlitz URL -->
```

Replace with:

```markdown
**[Live Demo](https://dielmannconsulting.github.io/ng-anim8/)**
```

- [ ] **Step 2: Commit and push**

```bash
git add README.md
git commit -m "docs: add GitHub Pages live demo URL [ci skip]"
git push
```

After pushing, the deploy job will run on GitHub Actions. Once it completes, the demo will be live at https://dielmannconsulting.github.io/ng-anim8/.

---

## Self-Review Notes

- All spec sections have a corresponding task: Visual Design (Task 2), Layout (Task 10), Sidebar (Task 10), Hero (Task 10), Animation Sections Tasks 4–9), Code Snippet Component (Task 3), GitHub Pages (Task 12), README (Task 13). ✓
- Type `Duration` is imported from `ng-anim8` in all section components. ✓
- `SlideDirection` is imported from `ng-anim8` in `SlideSectionComponent` only. ✓
- `CodeSnippetComponent` selector `app-code-snippet` is used consistently in all section templates. ✓
- `IntersectionObserver` guard in layout protects against jsdom test environment. ✓
- `404.html` is created via `cp` in the workflow, not as a committed file. ✓
- No TBDs, no similar-to-task-N shortcuts. ✓
